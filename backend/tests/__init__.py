"""Contract and security tests for the backend.

The application package is intentionally not imported from this module.  The
test suite is also useful while the API is being assembled, so imports happen
inside fixtures and helper resolvers and produce explicit skips when the
backend is not available yet.
"""
